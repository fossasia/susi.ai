import React from 'react';
import TextField from '@material-ui/core/TextField';

const OutlinedTextField = props => (
  <TextField margin="normal" variant="outlined" {...props} />
);

export default OutlinedTextField;
